// Q6. Write a Javascript program to calculate the Body Mass Index (BMI) using the formula BMI = weight (kg)/
// height * height.
// Ans:
    // const BMI = weight/ height * height

    const weight = 68;
    const height = 170.18/100;

    const bmi = weight/ (height * height);
    console.log(`Your BMI is : ${bmi}`);