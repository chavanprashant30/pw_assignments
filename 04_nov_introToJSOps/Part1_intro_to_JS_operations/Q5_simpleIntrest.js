// Q5. Write a JavaScript program that calculates the simple interest using the formula Simple interest =
// (principal * rate * time) / 100.
// Ans:
    // S.I. = (P × R × T)/100

    const p =5000;
    const r = 5;
    const t = 10;

    const simpleIntrest =(p * r * t)/100;
    console.log(`SimpleIntrest is : ${simpleIntrest}`);